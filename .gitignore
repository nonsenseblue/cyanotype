node_modules
dist
.DS_Store
*.local
.vite

# Env (none today, but keep secrets out by default)
.env
.env.local
.env.*.local

# Vercel CLI artifacts (contains project + token info)
.vercel

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental build cache
*.tsbuildinfo

# Editor
.vscode/
.idea/

# Photos live in Cloudflare R2 — keep all local copies out of git.
# originals: source files (locally only)
# large/thumbs: derived for upload, served from R2 in production
public/photos/**/originals/
public/photos/**/large/
public/photos/**/thumbs/
